{
  "name": "tcs-stock-prediction",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "install": "cd frontend && npm install",
    "build": "cd frontend && npm install && npm run build",
    "start": "npm --prefix frontend run preview"
  }
}
